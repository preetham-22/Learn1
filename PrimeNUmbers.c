include <stdio.h>
int main() {
    int firstNum,lastNum;
    printf("Enter the First Number and Last Number: ");
    scanf("%d %d",&firstNum,&lastNum);
    for(i=firstNum; i<= lastNum; i++){
        for(int j=0;j<Math.sqrt(i);j++){
            if(i)
        }
    }
}

